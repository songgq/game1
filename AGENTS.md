# 《翡翠纷争》项目永久开发约束

## 1. 客户可交付标准

- “代码可运行”“接口可调用”“自动化测试通过”不等于“可以交付给客户玩”。
- 宣称某个游戏阶段“完成”前，必须从真实玩家视角完整体验对应流程：进入页面、理解目标、执行操作、看到反馈、获得结果、继续下一步。
- 新功能如果只有数据表、接口、调试按钮或占位画面，只能标记为“技术骨架”或“内部原型”，禁止标记为“客户端完成”“可交付”或“已验收”。
- 新增客户端功能的完成度必须与现有正式战斗界面保持同一等级，不能把内部调试界面混入正式客户端。

## 2. 现有素材优先

- 开发任何新场景或界面前，必须先完整盘点、打开预览并分类项目已有素材，不能只根据文件名猜测用途。
- 能使用现有背景、地形、建筑、人物、资源、特效、按钮、面板、字体和音效时，必须优先复用，保持与战斗界面一致的美术语言。
- 未经用户明确同意，不得将 `ColorRect`、纯色几何图形、文字方块、程序员占位图或调试可视化作为正式游戏画面交付。
- 如果现有素材不足以达到正式效果，必须先向用户提交“已有素材可用项 + 缺失素材清单 + 建议替代方案”，确认后再继续。

## 3. 界面开发流程

- 正式UI开发顺序必须是：素材盘点 → 现有界面风格分析 → 页面结构/交互方案 → 可视化预览或截图 → 用户确认 → 正式实现 → 真机尺寸验收。
- 至少按720×1280基准尺寸验证，并检查不同手机长宽比下的布局、字体、触控区域、滚动、拖动和缩放。
- 每个新页面必须具备清晰的信息层级、正式素材、交互反馈、错误提示、加载状态、空状态和返回路径。
- 页面不能只展示服务端字段；必须转化为玩家能理解和愿意操作的游戏体验。

## 4. 玩法闭环

- 英雄升级、地块解锁、装备制作等成长功能上线前，必须确认玩家实际能够获得所需资源，并能理解资源来源。
- 如果资源产出阶段尚未开发，新成长功能不得以不可操作状态作为正式玩法交付；应隐藏入口、提供明确试玩数据，或先取得用户决定。
- 每个阶段验收必须同时覆盖：数据正确、服务端权威、客户端表现、操作反馈、资源闭环、历史兼容和玩家体验。

## 5. 验收与汇报

- 后台单元测试、数据库事务测试和接口测试只能证明工程正确性，不能替代视觉与玩法验收。
- 客户端交付至少需要：工程无错误、自动化测试、正式画面截图、完整操作录像或逐步体验记录，以及线上实际访问验证。
- 汇报时必须明确区分：未开始、技术骨架、内部原型、功能完成、视觉完成、玩法闭环完成、客户可交付。
- 不得为了显得进度快而夸大完成度；存在占位内容、缺少素材或无法形成玩法闭环时必须主动说明。

## 6. 发布约束

- 未经用户明确要求，不得部署线上客户端。
- 2026-08-23用户已明确授权：后续由用户提出的项目调整，在完成实现、测试和必要视觉验收后直接部署，无需逐次再次询问；如果用户明确要求“先不部署”“只讨论”或“先看方案”，则以该次要求为准。
- 即使用户要求按阶段持续开发，也不代表可以跳过视觉确认直接上线正式页面。
- 新客户端界面首次上线前，必须先向用户展示截图或可访问预览；用户确认后才能替换正式入口。
- 发布后必须验证缓存版本、静态资源、登录、核心页面、主要接口和服务日志。

## 7. 本次事故形成的硬性规则

- 2026-08-23的小岛色块界面属于失败案例：它只验证了坐标、缩放和接口，不具备正式美术与玩法品质，不得将类似实现再次称为可交付客户端。
- 战斗界面是本项目当前的正式品质基准。后续英雄、小岛、装备、PVE等界面必须以该基准设计和验收。
- 技术底座可以保留并继续演进，但技术底座与面向玩家的完成品必须分别评估、分别汇报。
